<?xml version="1.0" encoding="utf-8" ?>
<resources>

  <!-- Splash screen theme -->
  <style name="SplashTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <item name="android:windowBackground">@color/launcher_background</item>
    <item name="windowNoTitle">true</item>
    <item name="android:windowFullscreen">true</item>
    <item name="windowActionBar">false</item>
    
    <!--A cor da barra de aplicativos-->
    <item name="colorPrimary">@color/white</item>
    <!--A cor da barra de status e barras de aplicativos contextuais-->
    <item name="colorPrimaryDark">@color/black</item>
    <!--A cor dos controles de interface do usuário -->
    <item name="colorAccent">@color/white</item>
    <!--A cor de fundo da tela 
   
    A cor do texto de interface do usuário na barra de aplicativos
    <item name="android:textColorPrimary">@color/white</item>
    A cor da barra de status onde fica a hora e sinal 3G
    item name="android:statusBarColor">@color/white</item>
    A cor da barra de navegação onde fica a navegação do android
    <item name="android:navigationBarColor">@color/black</item>-->

    <!-- Disable activity slide-in animation -->
    <item name="android:windowAnimationStyle">@null</item>
  </style>

 

  <style name="MainTheme" parent="MainTheme.Base">
  </style>
  <!-- Base theme applied no matter what API -->
  <style name="MainTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
    <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
    <item name="windowNoTitle">true</item>
    <!--We will be using the toolbar so no need to show ActionBar-->
    <item name="windowActionBar">false</item>
    <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette -->
    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
    <item name="colorAccent">@color/colorAccent</item>
    <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight and colorSwitchThumbNormal. -->
    <item name="windowActionModeOverlay">true</item>

    <item name="android:datePickerDialogTheme">@style/AppCompatDialogStyle</item>
  </style>

  <style name="AppCompatDialogStyle" parent="Theme.AppCompat.Light.Dialog">
    <item name="colorAccent">@color/colorAccent</item>
  </style>
</resources>
